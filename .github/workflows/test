name: Test Runner

on:
  push:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Print machine
        run: uname -a

      - name: CPU info
        run: sysctl -n machdep.cpu.brand_string

      - name: Hello
        run: echo "Hello from Mac mini"
